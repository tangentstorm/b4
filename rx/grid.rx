needs vt' with vt' 
needs forth' with forth'

7 constant W
5 constant H

: dup2 2over ;
: @inc +!    ;

variables| i j |

: edge? W mod 0= ;
: . space 'c putc space ;

: run clear
  i 0 ! hex
  W H * for i ++
    i @ dup $10 < if '0 putc then putn space
    i @ edge? if cr then
  next
  decimal ;

run

