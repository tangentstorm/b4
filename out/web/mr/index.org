#+title: minrel

Minrel is a minimal relational graph database written in (/wait for it.../) object pascal.

This is its story.

- [[file:../mr0.pas.org][Prologue]] ::  In which we fire up pascal, create a 1-table database, and filter its rows and columns.
- [[file:../mr1.pas.org][Reification]] :: In which we build a table to describe a table.
- [[file:mr-boot.org][Bootstrap]] :: In which we build a database to describe a database.

